# randm_concept

## Быстрый запуск (платформы) 🚀
Краткая инструкция для запуска проекта на популярных платформах.

### Требования
- Установленный Flutter SDK (рекомендуется версия 3.0+). Проверьте: `flutter --version`.
- Android SDK / Xcode при разработке под Android/iOS соответственно.
- Для web: любой современный браузер.

### Запуск
- Android: `flutter run -d <device-id>` или откройте `android/` в Android Studio и запустите.
- iOS: `flutter run -d <device-id>` (на macOS с установленным Xcode).
- Web: `flutter run -d chrome` или `flutter run -d web-server`.
- Windows/Linux/macOS: `flutter run -d windows` / `flutter run -d linux` / `flutter run -d macos`.

### Тесты
- Unit/Widget tests: `flutter test`
- Анализ кода: `flutter analyze`